spring.application.name=queuein-backend
spring.jpa.hibernate.ddl-auto=create-drop
spring.datasource.url=${QUEUEIN_DB_URL}
spring.datasource.username=${QUEUEIN_DB_USERNAME}
spring.datasource.password=${QUEUEIN_DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
logging.level.org.hibernate.SQL=debug

